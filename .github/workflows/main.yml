on: push
name: 🚀 Deploy to Server
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.2
      with:
        server: 172.96.164.70
        username: TheLife
        password: ${{ secrets.ftp_password }}
        protocol: ftps
        port: 8821